name: Video Editor Pipeline

on:
  workflow_dispatch:
    inputs:
      json_spec:
        description: 'JSON Operation Spec'
        required: true

jobs:
  render-video:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v3

      - name: 🎥 Install FFmpeg (Full)
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 🚀 Run Video Editor
        # No arguments needed. 
        # Python will auto-detect it is running in Actions and read the input safely.
        run: |
          python video_editor.py

      - name: 📤 Upload Result
        uses: actions/upload-artifact@v4
        with:
          name: rendered-video
          path: output/
          retention-days: 1
